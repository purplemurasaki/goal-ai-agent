# Google OAuth（ローカル Supabase Auth）
# 1. GCP で OAuth 2.0 クライアント（Web）を作成
# 2. 承認済みリダイレクト URI: http://127.0.0.1:54321/auth/v1/callback
# 3. このファイルを .env にコピーして値を設定 → supabase stop && supabase start
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
